services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      ARCHIVE_DB: /data/archive.db
      DATA_DIR: /data
      HOST_DIR: /host
    volumes:
      - ./data:/data
      - .:/host:ro
    ports:
      - "${APP_PORT:-8010}:8000"
